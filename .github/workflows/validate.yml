name: Validate

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Validate PowerShell scripts
        shell: pwsh
        run: |
          $scripts = @("CyberVets-SSL-Attach.ps1", "push-to-github.ps1", "release-create.ps1")
          foreach ($s in $scripts) {
            $path = Join-Path (Get-Location) $s
            if (-not (Test-Path -LiteralPath $path)) { Write-Host "$s - Skipped (not found)"; continue }
            $errs = $null
            $null = [System.Management.Automation.Language.Parser]::ParseFile($path, [ref]$null, [ref]$errs)
            if ($null -ne $errs -and @($errs).Count -gt 0) { $errs | ForEach-Object { Write-Error $_ }; exit 1 }
            Write-Host "$s - Syntax OK"
          }

